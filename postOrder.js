const postorder = (tree) => {
    //traverse postorder
    if(tree.length == 1){
        console.log(tree.val);
    }
    let po = {'val': null, 'left': null, 'right': null }

    
}

postorder([1,2,3,4,5,6,7])